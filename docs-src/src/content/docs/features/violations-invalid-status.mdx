---
title: "Violation: invalid status"
description: "A note with explicit status outside `open`, `hold`, `closed`, or `archived` is invalid."
sidebar:
  label: "Violation: invalid status"
  order: 12
---

import { Tabs, TabItem } from '@astrojs/starlight/components';

A note with explicit status outside `open`, `hold`, `closed`, or `archived` is invalid.

## Rationale

### Why This Violation Exists

Status controls which workflow a note belongs to. A typo like `waiting` should
not silently remove the note from the expected board.

Inbox reports invalid explicit statuses while still allowing missing or blank
status to use the documented Open default.

## User Value

The user gets an immediate repair cue when a typo or obsolete status would otherwise hide a note from the intended workflow.

## Executable Documentation

- Explicit unsupported status values are reported in Inbox.
- Non-string status values are reported in Inbox.
- Missing or blank status does not trigger this violation because it defaults to Open.

## Screenshots

Screenshots are captured by WDIO in Obsidian Light, Obsidian Dark, and Dark with
the Minimal theme. Dark Minimal is shown first by default; use the tabs to switch
variants in the browser.

### Invalid status violation

<Tabs syncKey="feature-screenshot-theme">
<TabItem label="Dark Minimal">
  <figure class="feature-shot">
    <img src="../../assets/features/violations-invalid-status/dark-minimal/violation.png" alt="Inbox row showing an invalid status violation in Dark Minimal" loading="lazy" />
    <figcaption>Invalid status violation - Dark Minimal</figcaption>
  </figure>
</TabItem>
<TabItem label="Dark">
  <figure class="feature-shot">
    <img src="../../assets/features/violations-invalid-status/dark/violation.png" alt="Inbox row showing an invalid status violation in Dark" loading="lazy" />
    <figcaption>Invalid status violation - Dark</figcaption>
  </figure>
</TabItem>
<TabItem label="Light">
  <figure class="feature-shot">
    <img src="../../assets/features/violations-invalid-status/light/violation.png" alt="Inbox row showing an invalid status violation in Light" loading="lazy" />
    <figcaption>Invalid status violation - Light</figcaption>
  </figure>
</TabItem>
</Tabs>

## Fixtures

- `acceptance/fixtures/base/Db/Growth/Invalid Status.md`

