## Why Done Exists

Completed work is useful after the checkbox changes. Done gives that history a
focused mode where the user can review a day, switch dates, and see the work
grouped by the same contexts used on the boards without opening another view.

The mode reads completion dates from Tasks metadata so it does not need a
separate archive or log.
