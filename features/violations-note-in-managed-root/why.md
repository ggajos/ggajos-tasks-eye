## Why this issue exists

The notes folder root is a fast capture location, while descendant folders
provide the context Tasks Eye uses to organize work. A note left at the root has
not yet been routed to its owning context.

Inbox makes that cleanup step explicit. Moving the note into any
descendant folder resolves the issue without changing its Markdown.
