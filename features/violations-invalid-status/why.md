## Why this issue exists

Status controls which workflow a note belongs to. A typo like `waiting` should
not silently remove the note from the expected board.

Inbox reports unsupported explicit statuses while still allowing missing or
blank status to use the documented Open default.
